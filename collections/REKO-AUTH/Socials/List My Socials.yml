info:
  name: List My Socials
  type: http
  seq: 1

http:
  method: GET
  url: "{{REKO_AI_AUTH_URL}}/api/v1/socials/"
  auth:
    type: bearer
    token: "{{ACCESS_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
