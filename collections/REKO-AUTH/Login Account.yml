info:
  name: Login Account
  type: http
  seq: 2

http:
  method: POST
  url: "{{REKO_AI_AUTH_URL}}/api/v1/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "test@example.com",
        "password": "SecurePassword123!"
      }
  auth: inherit
