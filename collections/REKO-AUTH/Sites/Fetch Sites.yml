info:
  name: Fetch Sites
  type: http
  seq: 3

http:
  method: GET
  url: "{{REKO_AI_AUTH_URL}}/api/v1/sites/"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{ACCESS_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
