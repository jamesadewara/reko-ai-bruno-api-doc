info:
  name: "Complete User Onboarding "
  type: http
  seq: 11

http:
  method: POST
  url: "{{REKO_AI_AUTH_URL}}/api/v1/auth/onboard"
  auth:
    type: bearer
    token: "{{ACCESS_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
