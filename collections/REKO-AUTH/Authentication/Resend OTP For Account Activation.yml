info:
  name: Resend OTP For Account Activation
  type: http
  seq: 9

http:
  method: POST
  url: "{{REKO_AI_AUTH_URL}}/api/v1/auth/resend-otp"
  body:
    type: json
    data: |-
      {
        "email": "jamesadewara3@gmail.com",
        "purpose": "verify" //reset_password or verify
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
