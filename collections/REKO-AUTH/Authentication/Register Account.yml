info:
  name: Register Account
  type: http
  seq: 1

http:
  method: POST
  url: "{{REKO_AI_AUTH_URL}}/api/v1/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "jamesadewara3@gmail.com",
        "password": "SecurePassword123!",
        "name": "James",
        "date_of_birth": "2006-04-16",
        "confirm_password": "SecurePassword123!",
        "agreed_to_terms": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
