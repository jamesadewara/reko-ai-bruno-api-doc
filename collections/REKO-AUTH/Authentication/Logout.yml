info:
  name: Logout
  type: http
  seq: 6

http:
  method: POST
  url: "{{REKO_AI_AUTH_URL}}/api/v1/auth/logout"
  auth:
    type: bearer
    token: "{{ACCESS_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
