info:
  name: Reset Password
  type: http
  seq: 11

http:
  method: POST
  url: "{{REKO_AI_AUTH_URL}}/api/v1/auth/reset-password"
  body:
    type: json
    data: |-
      {
        "email": "jamesadewara3@gmail.com",
        "code": "746392",
        "password": "string234",
        "confirm_password": "string234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
