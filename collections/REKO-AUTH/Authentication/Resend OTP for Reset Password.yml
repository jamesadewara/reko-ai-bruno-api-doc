info:
  name: Resend OTP for Reset Password
  type: http
  seq: 3

http:
  method: POST
  url: "{{REKO_AI_AUTH_URL}}/api/v1/auth/resend-otp"
  body:
    type: json
    data: |-
      {
        "email": "jamesadewara3@gmail.com",
        "purpose": "reset_password" //reset_password or verify
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
