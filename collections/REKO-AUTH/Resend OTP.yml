info:
  name: Resend OTP
  type: http
  seq: 4

http:
  method: POST
  url: "{{REKO_AI_AUTH_URL}}/api/v1/auth/resend-otp"
  body:
    type: json
    data: |-
      {
        "email": "test@example.com",
        "purpose": "verify"
      }
  auth: inherit
