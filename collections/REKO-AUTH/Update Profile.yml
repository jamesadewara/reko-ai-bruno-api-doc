info:
  name: Update Profile
  type: http
  seq: 6

http:
  method: PUT
  url: "{{REKO_AI_AUTH_URL}}/api/v1/auth/profile"
  body:
    type: json
    data: |-
      {
        "name": "Updated Name",
        "date_of_birth": "1995-05-10"
      }
  auth: bearer

auth:
  bearer:
    token: "{{ACCESS_TOKEN}}"
