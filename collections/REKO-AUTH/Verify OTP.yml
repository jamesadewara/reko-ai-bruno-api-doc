info:
  name: Verify OTP
  type: http
  seq: 3

http:
  method: POST
  url: "{{REKO_AI_AUTH_URL}}/api/v1/auth/verify-otp"
  body:
    type: json
    data: |-
      {
        "email": "test@example.com",
        "code": "123456",
        "purpose": "verify"
      }
  auth: inherit
