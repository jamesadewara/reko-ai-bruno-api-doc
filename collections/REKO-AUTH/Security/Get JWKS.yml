info:
  name: Get JWKS
  type: http
  seq: 3

http:
  method: GET
  url: "{{REKO_AI_AUTH_URL}}/api/v1/.well-known/jwks.json"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
