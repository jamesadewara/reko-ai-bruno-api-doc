info:
  name: Verify Token
  type: http
  seq: 8

http:
  method: POST
  url: "{{REKO_AI_AUTH_URL}}/api/v1/auth/token/verify"
  body:
    type: json
    data: |-
      {
        "token": "{{ACCESS_TOKEN}}"
      }
  auth: inherit
