info:
  name: Revoke Token
  type: http
  seq: 9

http:
  method: POST
  url: "{{REKO_AI_AUTH_URL}}/api/v1/auth/token/revoke"
  body:
    type: json
    data: |-
      {
        "token": "{{ACCESS_TOKEN}}"
      }
  auth: inherit
