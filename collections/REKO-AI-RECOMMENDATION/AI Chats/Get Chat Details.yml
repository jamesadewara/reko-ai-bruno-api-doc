info:
  name: Get Chat Details
  type: http
  seq: 6

http:
  method: GET
  url: "{{REKO_AI_SYSTEM_URL}}/api/v1/chats/:chatId"
  auth:
    type: bearer
    token: "{{ACCESS_TOKEN}}"
  params:
    path:
      chatId: 

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
