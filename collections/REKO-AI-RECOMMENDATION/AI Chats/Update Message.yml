info:
  name: Update Message
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{REKO_AI_SYSTEM_URL}}/api/v1/chats/:chatId/messages/:messageId"
  auth:
    type: bearer
    token: "{{ACCESS_TOKEN}}"
  body:
    type: json
    json: |
      {
        "content": "Updated message content"
      }
  params:
    path:
      chatId: 
      messageId: 

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
