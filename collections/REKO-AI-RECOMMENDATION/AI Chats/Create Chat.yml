info:
  name: Create Chat
  type: http
  seq: 2

http:
  method: POST
  url: "{{REKO_AI_SYSTEM_URL}}/api/v1/chats"
  auth:
    type: bearer
    token: "{{ACCESS_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
