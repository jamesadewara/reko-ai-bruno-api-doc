info:
  name: Get Chat Flow
  type: http
  seq: 9

http:
  method: GET
  url: "{{REKO_AI_SYSTEM_URL}}/api/v1/chats/flows/:mode"
  auth:
    type: bearer
    token: "{{ACCESS_TOKEN}}"
  params:
    path:
      mode: 

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
