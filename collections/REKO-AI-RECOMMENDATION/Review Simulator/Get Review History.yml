info:
  name: Get Review History
  type: http
  seq: 3

http:
  method: GET
  url: "{{REKO_AI_SYSTEM_URL}}/api/v1/reviews/history"
  auth:
    type: bearer
    token: "{{ACCESS_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
