info:
  name: Get Review Style
  type: http
  seq: 2

http:
  method: GET
  url: "{{REKO_AI_SYSTEM_URL}}/api/v1/reviews/style"
  auth:
    type: bearer
    token: "{{ACCESS_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
